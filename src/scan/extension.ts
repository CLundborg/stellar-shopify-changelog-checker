import type { Signal } from "../types.js";

export async function scanExtensionSignals(
  _rootDir: string,
): Promise<Signal[]> {
  throw new Error("scanExtensionSignals not implemented yet; lands in M6");
}
