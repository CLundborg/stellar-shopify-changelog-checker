import type { Signal } from "../types.js";

export async function scanRemixAppSignals(_rootDir: string): Promise<Signal[]> {
  throw new Error("scanRemixAppSignals not implemented yet; lands in M4");
}
